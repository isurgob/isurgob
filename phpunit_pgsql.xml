<?xml version="1.0" encoding="UTF-8"?>

<phpunit bootstrap="Tests/bootstrap.php" colors="true">
    <php>
        <var name="db_dsn" value="pgsql:dbname=isurgob;host=localhost"/>
        <var name="db_username" value="webusr"/>
        <var name="db_password" value="test"/>
    </php>

    <testsuites>
        <testsuite name="Bienvenidos a ISURGOB Test">
            <!--<directory>./Tests/</directory>-->
        </testsuite>
    </testsuites>

    <filter>
        <whitelist>
            <directory>./</directory>
            <exclude>
                <directory>./Tests</directory>
            </exclude>
        </whitelist>
    </filter>
</phpunit>
